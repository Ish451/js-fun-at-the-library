function createTitle(title) {
  return `The ${title}`;

}

function buildMainCharacter(name)

return


var  = (Vassya")

module.exports = {

  createTitle: createTitle,


  // buildMainCharacter: buildMainCharacter,
  // saveReview: saveReview,
  // calculatePageCount: calculatePageCount,
  // writeBook: writeBook,
  // editBook: editBook
}
